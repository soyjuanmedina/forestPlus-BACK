name: Build & Deploy Spring Boot App

on:
  push:
    branches:
      - master
      - development

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ---------- Checkout backend ----------
      - name: Checkout backend repository
        uses: actions/checkout@v4

      # ---------- Determine branch and deploy path ----------
      - name: Set deploy path and frontend branch
        id: deploy-branch
        run: |
          if [[ "${GITHUB_REF_NAME}" == "development" ]]; then
            echo "DEPLOY_PATH=/appservers/apache-tomcat-10x/webapps/forestplus-dev" >> $GITHUB_ENV
            echo "FRONT_BRANCH=development" >> $GITHUB_ENV
          else
            echo "DEPLOY_PATH=/appservers/apache-tomcat-10x/webapps/ROOT" >> $GITHUB_ENV
            echo "FRONT_BRANCH=main" >> $GITHUB_ENV
          fi

      # ---------- Clone Angular Frontend ----------
      - name: Clone Angular Frontend
        run: |
          git clone --branch $FRONT_BRANCH https://github.com/soyjuanmedina/forestPlus-FRONT frontend

      # ---------- Node ----------
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # ---------- Build Angular ----------
      - name: Install Angular dependencies
        run: |
          cd frontend
          npm ci

      - name: Build Angular
        run: |
          cd frontend
          npm run build -- --configuration=production

      # ---------- Copy Angular build into Spring Boot static ----------
      - name: Copy Angular build to Spring Boot static
        run: |
          rm -rf src/main/resources/static/*
          cp -r frontend/dist/forest-plus/browser/* src/main/resources/static/

      # ---------- JAVA ----------
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Build with Maven
        run: mvn clean package -DskipTests

      # ---------- Deploy via lftp ----------
      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy WAR via lftp
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          DEPLOY_PATH: ${{ env.DEPLOY_PATH }}
        run: |
          WAR_FILE=$(find target -name "*.war" | head -n 1)
          echo "Deploying $WAR_FILE to $DEPLOY_PATH"

          lftp -c "
            set ftp:ssl-allow true
            set ftp:ssl-force true
            set ftp:ssl-protect-data true
            set ftp:passive-mode true
            set ssl:verify-certificate no
            open -u $FTP_USERNAME,$FTP_PASSWORD $FTP_SERVER

            rm -f $DEPLOY_PATH/*.war || true
            put -O $DEPLOY_PATH $WAR_FILE
          "
